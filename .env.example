# ScaleDown API Configuration (REQUIRED)
# Get your API key from: https://scaledown.xyz
SCALEDOWN_API_KEY=your_scaledown_api_key_here
SCALEDOWN_API_URL=https://api.scaledown.xyz/compress/raw/

# OpenAI API Configuration (OPTIONAL - system works without it)
# Get your API key from: https://platform.openai.com/api-keys
# The system has a smart fallback mode if you don't configure this
OPENAI_API_KEY=your_openai_api_key_here
TARGET_MODEL=gpt-4o-mini

# Server Configuration (for hosting platforms)
# Leave as default for local development
# Hosting platforms like Render, Railway, etc. will override PORT automatically
HOST=0.0.0.0
PORT=8001
